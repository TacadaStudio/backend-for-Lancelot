services:
  - type: web
    name: lancelot-backend
    env: docker
    rootDir: backend
    dockerfilePath: Dockerfile
    envVars:
      - key: ALLOWED_ORIGINS
        value: "https://lancelot-extractor-tablas-pdf.onrender.com" # Default placeholder for your frontend
